import SwiftUI

struct BagSettingsView: View {
    var body: some View {
        Form {
            Section {
                Text("整理書包設定即將推出")
                    .foregroundStyle(.secondary)
            }
        }
        .navigationTitle("整理書包")
    }
}
