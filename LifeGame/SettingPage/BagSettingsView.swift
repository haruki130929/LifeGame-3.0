import SwiftUI

struct BagSettingsView: View {
    var body: some View {
        Form {
            Section {
                Text("收拾書包設定即將推出")
                    .foregroundStyle(.secondary)
            }
        }
        .navigationTitle("收拾書包")
    }
}
