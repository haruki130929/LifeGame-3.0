import SwiftUI

struct MonthlyScoreSettingsView: View {
    var body: some View {
        Form {
            Section {
                Text("本月結算設定即將推出")
                    .foregroundStyle(.secondary)
            }
        }
        .navigationTitle("本月結算")
    }
}
